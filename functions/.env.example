# ──────────────────────────────────────────────────────────────────────────
# Childbooks backend (Firebase Functions) — environment template
#
# These are the secrets/config the BACKEND owns. In production they live in
# Cloud Secret Manager (see functions:secrets:set) and are injected into the
# runtime automatically. For LOCAL emulator development, copy this file to
# `.env.local` (git-ignored) and fill in real values — the emulator loads it
# into process.env, where `loadServerConfig(process.env)` reads them.
#
#     cp functions/.env.example functions/.env.local
# ──────────────────────────────────────────────────────────────────────────

# ── AI providers ───────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-dummy-openai-key-replace-me
GOOGLE_API_KEY=AIzaSy-dummy-gemini-key-replace-me

# ── Generation fan-out (Cloud Tasks) ───────────────────────────────────────
# The image worker (`runFanTask`) is a Cloud Tasks queue: one task per unit
# (page/cover/anchor). These tune the queue at DEPLOY time (read from the env at
# module load). Sensible defaults are baked in; override to trade speed against
# provider rate limits / cost.
#   FAN_MAX_CONCURRENT   — max tasks running across the whole fleet at once. This
#                          is the single knob that shields the image provider
#                          from bursts, no matter how many tasks are enqueued.
#                          Set at/below your provider's concurrency budget. (20)
#   FAN_MAX_PER_SEC      — max task dispatch rate per second. (10)
#   FAN_WORKER_CONCURRENCY — tasks one warm instance serves in parallel (fewer
#                          cold starts, but shares the 2 GiB memory budget). (4)
#   FAN_MAX_ATTEMPTS     — Cloud Tasks retries for INFRA failures (worker crash /
#                          OOM / timeout); render failures are terminal. (3)
# FAN_MAX_CONCURRENT=20
# FAN_MAX_PER_SEC=10
# FAN_WORKER_CONCURRENCY=4
# FAN_MAX_ATTEMPTS=3

# ── Guest (anonymous user) cleanup ─────────────────────────────────────────
# The `cleanupAnonymousUsers` scheduled function deletes anonymous accounts (and
# their Firestore/Storage data) that have been inactive for this many days.
# Defaults to 30 when unset. Lower it to drain a backlog faster, raise it to keep
# returning guests longer.
# ANON_CLEANUP_MAX_AGE_DAYS=30

# ── Cost suggestion (admin "Suggest" button) ──────────────────────────────
# Official pricing pages the admin cost-suggester reads with a cheap text model.
# Defaults below are baked in; override only if a provider moves its page (so no
# code change is needed). New models work automatically — they're matched by id
# in the live page, nothing is hardcoded per model.
# GOOGLE_PRICING_URL=https://ai.google.dev/gemini-api/docs/pricing
# OPENAI_PRICING_URL=https://developers.openai.com/api/docs/pricing

# ── Lulu (print-on-demand) ─────────────────────────────────────────────────
# Lulu issues SEPARATE OAuth credentials per environment: the sandbox pair only
# works against api.sandbox.lulu.com, the live pair only against api.lulu.com.
# Set both pairs; the one matching LULU_ENV is used.
LULU_SANDBOX_CLIENT_KEY=dummy-lulu-sandbox-client-key-replace-me
LULU_SANDBOX_CLIENT_SECRET=dummy-lulu-sandbox-client-secret-replace-me
LULU_LIVE_CLIENT_KEY=dummy-lulu-live-client-key-replace-me
LULU_LIVE_CLIENT_SECRET=dummy-lulu-live-client-secret-replace-me
# "sandbox" (test) or "live" (real orders + charges). Keep on sandbox.
LULU_ENV=sandbox

# Legacy single-pair fallback (used for whichever LULU_ENV is active if the
# env-specific vars above are unset). Prefer the *_SANDBOX_*/*_LIVE_* pairs.
# LULU_CLIENT_KEY=
# LULU_CLIENT_SECRET=

# ── Stripe (payments) ──────────────────────────────────────────────────────
# Stripe holds a SECRET API key + a webhook SIGNING secret per environment. The
# sandbox/test pair (sk_test_…, whsec_…) only works in test mode; the live pair
# (sk_live_…) takes real money. Set both; the one matching STRIPE_ENV is used.
#
# STRIPE_ENV mirrors LULU_ENV when unset, so a single switch flips the whole
# backend between sandbox and live. Keep on sandbox for development.
STRIPE_ENV=sandbox
STRIPE_SANDBOX_SECRET_KEY=sk_test_dummy-stripe-secret-replace-me
# The webhook signing secret. In LOCAL dev `yarn dev:backend --stripe` starts the
# Stripe CLI listener and injects this automatically — leave it blank locally. In
# production, set it to the signing secret of the registered webhook endpoint.
STRIPE_SANDBOX_WEBHOOK_SECRET=
STRIPE_LIVE_SECRET_KEY=sk_live_dummy-stripe-secret-replace-me
STRIPE_LIVE_WEBHOOK_SECRET=whsec_dummy-live-webhook-secret-replace-me

# Public base URL of the storefront, used for Stripe Checkout success/cancel
# redirects and the Customer Portal return URL. Defaults to http://localhost:3000
# when unset (fine for local dev).
# PUBLIC_APP_URL=https://your-app.example.com

# ── Asset hosting / generated images (Firebase Storage via Admin SDK) ──────
# The backend uploads generated images + print files as admin using Application
# Default Credentials (NO private key file needed). This MUST match the client's
# NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET exactly — otherwise images are written to a
# bucket the browser can't read and show up empty. Defaults to
# "<projectId>.firebasestorage.app" when unset.
# NOTE: the FIREBASE_ prefix is reserved by Firebase and can't be set in a
# functions .env file, so the override uses the unprefixed STORAGE_BUCKET.
# STORAGE_BUCKET=your-project-id.firebasestorage.app

# ── Storage in local development ───────────────────────────────────────────
# Local dev uses the REAL bucket; only Auth, Firestore, Functions and Pub/Sub are
# emulated. That's deliberate, and it's why no tunnel (ngrok etc.) is needed:
#   - Lulu DOWNLOADS the interior/cover PDFs minutes after accepting a job, so
#     their URLs have to be reachable from outside your machine.
#   - Ebook download links have to keep working after a restart.
# Emulated Storage serves `http://127.0.0.1:9199/...`, which satisfies neither.
#
# This needs Application Default Credentials. `yarn dev:backend` checks them at
# startup and offers to sign you in, so there's normally nothing to do. They live
# in the repo's own `.gcloud/` dir (git-ignored) rather than machine-wide, so this
# project can't repoint the credentials your other Google projects use. Manually:
#   CLOUDSDK_CONFIG=.gcloud gcloud auth application-default login
#
# Set this to emulate Storage anyway (useful offline). Print checkout then refuses
# the order at its pre-payment reachability check, by design; everything else
# works. Must be mirrored by NEXT_PUBLIC_USE_STORAGE_EMULATOR on the frontend, or
# the browser reads a different bucket than the backend writes.
# USE_STORAGE_EMULATOR=true

# ── Order status: webhook (prod) + polling (local) ─────────────────────────
# The provider (Lulu) POSTs order-status updates to our `/print-webhook` receiver,
# which verifies the `Lulu-HMAC-SHA256` signature using the SAME client secret
# above — no extra secret to configure.
#
#   - Local dev: nothing to configure. Lulu can't reach localhost, so
#     `yarn dev:order` (or `--order`) PULLS status every 30s via the emulator-only
#     `/internal/print/sync` route, through the same reducer the webhook uses.
#   - Production: register the deployed receiver once per environment via the
#     admin endpoint `POST /admin/print/webhooks` { "url": "https://<host>/print-webhook" }
#     (sandbox + live use separate credentials, so register under each). Manage
#     with GET/DELETE `/admin/print/webhooks` and test via `.../:id/test`.
#     The `syncPrintOrders` scheduled function reconciles every 15 min regardless,
#     so a missed or deactivated webhook can't strand an order; `POST
#     /admin/print/sync` runs the same reconcile on demand.


# ZeptoMail (Zoho's transactional email service). Same account serves dev + prod
# (no sandbox/live split). Get these from the ZeptoMail console → Mail Agent → API.
ZEPTOMAIL_TOKEN=Zoho-enczapikey wSsVR60h+rl+XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX==
ZEPTOMAIL_WEBHOOK_SECRET=replace-with-a-random-string
# Only needed if your account is NOT on the US data center (the console's API
# tab shows your exact endpoint). Leave unset to use the US default.
# EU:    https://api.zeptomail.eu/v1.1/email
# India: https://api.zeptomail.in/v1.1/email
ZEPTOMAIL_API_URL=https://api.zeptomail.com/v1.1/email

# ── Slack notifications ────────────────────────────────────────────────────
# Incoming-webhook URL for event pings — new signups, purchases, new
# subscribers, and operational alerts (see functions/src/notify.ts). Prod-only
# and best-effort: leave blank locally (the emulator never pings Slack).
#
# THIS FILE IS THE SINGLE SOURCE OF TRUTH. `yarn setSecrets` pushes it BOTH to
# Cloud Secret Manager (the running functions read it) AND to GitHub Actions
# secrets (the CI/CD deploy + CI workflows ping it) — edit it here, run once.
#
# Create one at https://api.slack.com/apps → Incoming Webhooks → Add New Webhook
# to Workspace, pick the channel, copy the https://hooks.slack.com/services/...
# URL. No bot token or channel id needed — the webhook is bound to one channel.
SLACK_WEBHOOK_URL=
# Optional: a SEPARATE webhook for operational alerts (errors, refunds, low
# balances). Leave blank to send ops alerts to SLACK_WEBHOOK_URL too. (Note: to
# actually use a split channel you must also add it to secrets.ts / setSecrets.)
# SLACK_OPS_WEBHOOK_URL=