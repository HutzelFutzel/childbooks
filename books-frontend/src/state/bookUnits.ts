/**
 * Re-export of the platform-agnostic book-unit helpers (now in core), kept here
 * so existing UI/state imports remain stable.
 */
export {
  coverSpread,
  illustrationUnits,
  spreadsById,
} from "../core/book/units";
