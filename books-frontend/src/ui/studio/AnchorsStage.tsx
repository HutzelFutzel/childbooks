/**
 * Design · Cast entry — the casting workspace shares Pages chrome
 * (strip / stage / dock). Kept as a thin re-export so existing imports stay stable.
 */
export { CastWorkspace as AnchorsStage } from "./CastWorkspace";
